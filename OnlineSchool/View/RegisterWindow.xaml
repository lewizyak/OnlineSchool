<Window x:Class="OnlineSchool.View.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OnlineSchool.View"
        mc:Ignorable="d"
        Title="RegisterWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ФИО"/>
        <TextBox x:Name="tbName" Grid.Row="1" Height="30" Margin="0,6,0,12"/>

        <TextBlock Grid.Row="2" Text="Телефон"/>
        <TextBox x:Name="tbPhone" Grid.Row="3" Height="30" Margin="0,6,0,12"/>

        <TextBlock Grid.Row="4" Text="Пароль"/>
        <PasswordBox x:Name="pbPassword" Grid.Row="5" Height="30" Margin="0,6,0,12" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom">
            <Button Content="Отмена"
                    Width="100"
                    Margin="0,0,10,0"
                    Click="BtnCancel_Click"/>

            <Button Content="Зарегистрироваться"
                    Width="160"
                    Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>