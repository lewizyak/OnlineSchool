<Window x:Class="OnlineSchool.View.TeacherGradesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OnlineSchool.View"
        mc:Ignorable="d"
        Title="TeacherGradesWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="dgStudents" Grid.Row="0" IsReadOnly="True"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
            <TextBlock Text="Оценка:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="tbGrade" Width="80" Margin="0,0,20,0"/>

            <TextBlock Text="Комментарий:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="tbComment" Width="250"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Обновить" Width="120" Margin="0,0,10,0" Click="BtnRefresh_Click"/>
            <Button Content="Поставить оценку" Width="150" Margin="0,0,10,0" Click="BtnSaveGrade_Click"/>
            <Button Content="Закрыть" Width="120" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
